%h1 Add new product

= simple_form_for @product do |f|
	= f.input :name
	= f.input :calories
	= f.input :carbohydrates
	= f.input :protein
	= f.input :fat
	= f.button :submit, class: 'btn btn-success'

